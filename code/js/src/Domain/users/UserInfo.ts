/**
 * `UserInfo` is a type that represents the information of a user.
 * It includes properties for `username`, `wins`, `losses`, `draws`, and `gamesPlayed`.
 */
export type UserInfo = {
  username: string;
  wins: number;
  losses: number;
  draws: number;
  gamesPlayed: number;
};
